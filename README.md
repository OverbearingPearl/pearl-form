# pearl-form

pearl-form is a browser-native structured Lisp environment with X-ray vision. It doesn't just run your code — it lets you watch it think: characters splitting into tokens, tokens magnetizing into an AST, macros folding open like origami, and recursive calls blooming into a living tree. Core interpreter in Zig, compiled to WASM, zero frameworks, zero backends.
